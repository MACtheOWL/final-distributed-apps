name: distributed-apps-final

services:
    order-service:
        build: ./order-service
        restart: always
        depends_on:
            - product-service
        ports:
            - 3000:8080

    product-service:
        restart: always
        build: ./product-service
        ports:
            - 3001:8080
